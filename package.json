{
  "name": "univursyl",
  "version": "1.0.0",
  "description": "Premium space science media — the universe, explained.",
  "type": "module",
  "scripts": {
    "fetch:news":    "node scripts/fetch-news.js",
    "fetch:apod":    "node scripts/fetch-apod.js",
    "fetch:launches":"node scripts/fetch-launches.js",
    "build":         "node scripts/build-pages.js",
    "pipeline":      "npm run fetch:news && npm run fetch:apod && npm run fetch:launches && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "node-fetch":        "^3.3.2",
    "rss-parser":        "^3.13.0"
  }
}
